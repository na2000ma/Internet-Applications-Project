import {FieldType} from '@ngx-formly/core';
import {Component} from "@angular/core";

@Component({
  selector: 'app-base-field',
  template: ''
})
export class BaseFieldComponent extends FieldType {
}
